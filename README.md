# ocp

OpenCode Configuration Profiles CLI.

## Requirements

- Java 25

## Build

```bash
./gradlew build
```

## Run help

```bash
./gradlew run --args="help"
```

## Build native executable

```bash
./gradlew nativeCompile
```

The resulting binary is generated as `build/native/nativeCompile/ocp`.
